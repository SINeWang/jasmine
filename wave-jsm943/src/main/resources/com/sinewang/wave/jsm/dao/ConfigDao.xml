<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sinewang.wave.jsm.dao.ConfigDao">
    <cache/>
    <select id="getAllConfigs" resultType="Config">
        SELECT * FROM extapi_config WHERE namespace = #{namespace}
    </select>
</mapper>